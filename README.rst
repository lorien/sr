Console tool to search & replace content in files
=================================================

How to use
----------

Search for FOO string in file in current directory and
all subdirectories:

.. code:: shell

    sr FOO

Search for FOO string in file in current directory and
all subdirectories and replace found string with BAR string:

.. code:: shell

    sr FOO -r BAR
